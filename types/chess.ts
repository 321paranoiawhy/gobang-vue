/* 交叉点落子情况 */
export enum ChessEnum {
  None,
  Black,
  White
}

/* 行号显示位置 */
export type TRowNumberPosition = 'left' | 'right';
/* 列号显示位置 */
export type TColumnNumberPosition = 'top' | 'bottom';
